# sops-nix secret files — real secrets never enter the repo.
# The *.example templates stay tracked; these two files are generated
# locally by setup.sh and must stay out of git.
.sops.yaml
secrets.yaml