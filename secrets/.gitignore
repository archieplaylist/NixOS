# sops-nix secret files — age-encrypted ciphertext is safe to track in git.
# Private keys stay in /root/.config/sops/age/keys.txt (outside the repo).
# Add unencrypted temporary or raw secret files below if any.
*.decrypted.*
*.unencrypted.*